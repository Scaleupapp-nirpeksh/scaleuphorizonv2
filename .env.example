# ==============================================
# ScaleUp Horizon Backend v3.0 - Environment Variables
# ==============================================

# Server Configuration
NODE_ENV=development
PORT=5001
API_VERSION=v1

# Database
MONGODB_URI=mongodb://localhost:27017/scaleup_horizon_v3

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# AWS S3 (for file uploads)
# Required for file upload functionality (avatars, receipts, documents)
# Create an S3 bucket and IAM user with S3 access
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=ap-south-1
S3_BUCKET_NAME=scaleup-horizon-uploads
# S3 Bucket CORS Configuration needed:
# AllowedOrigins: ["*"] or your frontend URL
# AllowedMethods: ["GET", "PUT", "POST", "DELETE"]
# AllowedHeaders: ["*"]

# Email (SMTP)
# For Gmail, use an App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=ScaleUp Horizon

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=debug

# OpenAI (for AI-powered features)
# Required for categorization, predictions, and NLP queries
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=4000
